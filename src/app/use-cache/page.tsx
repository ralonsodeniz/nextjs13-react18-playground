import FullName from '@/app/use-cache/components/FullName';

export const metadata = {
  title: 'use hook and cache',
  description: 'client side use hook and cache example',
};

const UseCachePage = () => {
  return <FullName />;
};

export default UseCachePage;
